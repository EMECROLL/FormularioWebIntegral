import React from "react";

const Home = () => {
  return (
    <div>
        Hola, bienvenido a la página de inicio de nuestro formulario web integral.

        <button onClick={() => window.location.href="/Formulario"}>Formulario</button>
    </div>
  );
};

export default Home;
